{
  "name": "styled-components-project",
  "private": true,
  "scripts": {
    "build": "pnpm run --filter=styled-components build",
    "postbuild": "cp README.md packages/styled-components/",
    "postinstall": "pnpm run --filter=styled-components generateErrors",
    "lint": "pnpm run --filter=styled-components lint",
    "size": "pnpm run --filter=styled-components lint:size",
    "test": "pnpm run --filter=styled-components test"
  },
  "author": "Glen Maddern",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/styled-components/styled-components/issues"
  },
  "homepage": "https://styled-components.com",
  "engines": {
    "node": ">= 14"
  },
  "dependencies": {}
}
